' TestFile: alphabetical_testing.xlsx
' MainFile: Multiple_year_stock_data.xlsx

' Create a script that will loop through all the stocks for one year and _
' output the following information.

' (1) The ticker symbol

Dim WhichYear As Integer
Dim Symbol As String

Dim Sheet As Worksheet
    Set Sheet = ThisWorkbook.Sheets("Sheet1")

for x

' (2) Yearly change from opening price at the beginning of a given year _
' to the closing price at the end of that year.

' (3) The percent change from opening price at the beginning of a given year _
' to the closing price at the end of that year.

' (4) The total stock volume of the stock.

' (5) You should also have conditional formatting that will highlight _
'     positive change in green and negative change in red.





